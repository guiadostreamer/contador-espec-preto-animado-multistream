/*
===============================================================================
 CONTADOR DE ESPECTADORES GUIA PRETO ANIMADO MULTISTREAM — CSS
===============================================================================

 Copyright © 2026 Leonardo — Guia do Streamer.
 Todos os direitos reservados.

 Projeto: Contador de Espectadores Guia Preto Animado Multistream
 Edição: Black Outline Animado horizontal
 Versão: 1.0.0
 Identificador: GDS-VC-PRETO-ANIM-260908-917364-CSS

 Direção criativa, conceito, personalização, testes e publicação:
 Leonardo — Guia do Streamer

 Desenvolvimento técnico realizado com auxílio de Inteligência Artificial.

 LICENÇA DE USO GRATUITO COM RESTRIÇÕES

 É PERMITIDO:
 - Usar este código em suas próprias transmissões pessoais ou monetizadas.
 - Personalizar e modificar o código exclusivamente para uso próprio.

 NÃO É PERMITIDO:
 - Redistribuir, republicar, revender, sublicenciar ou fazer reupload.
 - Disponibilizar o código original ou modificado em outros repositórios,
   sites, vídeos, grupos, packs, downloads ou plataformas.
 - Remover ou alterar este aviso, os créditos ou a identificação do projeto.
 - Apresentar este projeto, layout, animações ou código como criação própria.

 Para compartilhar, envie somente o link da publicação ou do repositório
 oficial do Guia do Streamer.

 Os termos completos estão no README.md e no LICENSE.txt deste projeto.
 A ausência deste aviso em uma cópia não representa autorização de uso.

===============================================================================
*/

* {
  box-sizing: border-box;
}

html,
body {
  width: 100%;
  height: 100%;
  margin: 0;
  overflow: hidden;
  background: transparent !important;
}

#viewer_widget {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 28px;
  font-family: "Open Sans", Arial, sans-serif;
  animation: widget-enter 700ms cubic-bezier(0.2, 0.85, 0.25, 1) both;
}

.viewer-black {
  position: relative;
  display: inline-flex;
  align-items: center;
  gap: 18px;
  min-height: 88px;
  max-width: 100%;
  padding: 16px 20px;
  overflow: hidden;
  color: #ffffff;
  background: #000000;
  border: 1px solid rgba(255, 255, 255, 0.58);
  border-radius: 20px;
  box-shadow:
    0 18px 44px rgba(0, 0, 0, 0.52),
    inset 0 0 0 1px rgba(255, 255, 255, 0.045);
  transition: border-color 260ms ease, filter 260ms ease;
  animation: hud-float 4.8s ease-in-out 850ms infinite;
}

.viewer-black::before {
  content: "";
  position: absolute;
  top: 0;
  left: 12%;
  width: 48%;
  height: 1px;
  background: linear-gradient(
    90deg,
    transparent,
    rgba(255, 255, 255, 0.95),
    transparent
  );
}

.viewer-black::after {
  content: "";
  position: absolute;
  z-index: 0;
  top: -65%;
  left: -38%;
  width: 14%;
  height: 230%;
  pointer-events: none;
  opacity: 0;
  transform: skewX(-18deg);
  background: linear-gradient(
    90deg,
    transparent,
    rgba(255, 255, 255, 0.14),
    transparent
  );
  animation: light-sweep 4.8s ease-in-out 1.2s infinite;
}

.viewer-black > * {
  position: relative;
  z-index: 1;
}

.live-status {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  flex: 0 0 auto;
  padding: 8px 11px;
  color: #ff5269;
  background: #000000;
  border: 1px solid rgba(255, 255, 255, 0.42);
  border-radius: 999px;
  font-size: 12px;
  font-weight: 800;
  letter-spacing: 0.12em;
  animation: live-badge 2s ease-in-out infinite;
}

.live-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: #ff4057;
  box-shadow: 0 0 0 0 rgba(255, 64, 87, 0.55);
  animation: live-pulse 1.7s infinite;
}

.total-viewers {
  display: grid;
  grid-template-columns: auto auto;
  grid-template-rows: auto auto;
  column-gap: 9px;
  align-items: center;
  flex: 0 0 auto;
  min-width: 112px;
}

.eye-icon {
  grid-row: 1 / 3;
  width: 25px;
  height: 25px;
  fill: none;
  stroke: #ffffff;
  stroke-width: 1.8;
  filter: drop-shadow(0 0 7px rgba(255, 255, 255, 0.28));
}

#total_count {
  min-width: 1ch;
  color: #ffffff;
  font-size: 28px;
  font-weight: 800;
  line-height: 0.95;
  font-variant-numeric: tabular-nums;
}

.total-label {
  margin-top: 3px;
  color: rgba(255, 255, 255, 0.62);
  font-size: 10px;
  font-weight: 700;
  line-height: 1;
  letter-spacing: 0.07em;
  text-transform: uppercase;
}

.divider {
  width: 1px;
  height: 42px;
  flex: 0 0 auto;
  background: linear-gradient(
    transparent,
    rgba(255, 255, 255, 0.72),
    transparent
  );
}

#platform_container {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 9px;
  margin: 0;
}

.platform-item {
  display: inline-flex !important;
  align-items: center;
  justify-content: center;
  gap: 7px;
  min-width: 62px;
  min-height: 42px;
  margin: 0 !important;
  padding: 8px 11px;
  color: #ffffff;
  background: #000000;
  border: 1px solid rgba(255, 255, 255, 0.44);
  border-radius: 12px;
  font-size: 19px !important;
  font-weight: 800 !important;
  font-variant-numeric: tabular-nums;
  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.1);
  transition: transform 180ms ease, border-color 180ms ease;
  animation: platform-enter 480ms cubic-bezier(0.2, 0.85, 0.25, 1) backwards;
}

.platform-item:nth-child(1) { animation-delay: 250ms; }
.platform-item:nth-child(2) { animation-delay: 340ms; }
.platform-item:nth-child(3) { animation-delay: 430ms; }
.platform-item:nth-child(4) { animation-delay: 520ms; }

.platform-item:hover {
  transform: translateY(-1px);
  border-color: rgba(255, 255, 255, 0.75);
}

.platform-icon {
  font-size: 20px;
  line-height: 1;
  filter: drop-shadow(0 0 6px currentColor);
  animation: icon-float 2.8s ease-in-out infinite;
}

.platform-img {
  width: 20px !important;
  height: 20px !important;
  object-fit: contain;
  animation: icon-float 2.8s ease-in-out infinite;
}

.platform-item:nth-child(2) .platform-icon,
.platform-item:nth-child(2) .platform-img { animation-delay: 350ms; }

.platform-item:nth-child(3) .platform-icon,
.platform-item:nth-child(3) .platform-img { animation-delay: 700ms; }

.platform-item:nth-child(4) .platform-icon,
.platform-item:nth-child(4) .platform-img { animation-delay: 1050ms; }

.platform-img:not([src]),
.platform-img[src=""] {
  display: none;
}

.viewer-black.viewer-up {
  border-color: rgba(83, 252, 24, 0.72);
  filter: drop-shadow(0 0 12px rgba(83, 252, 24, 0.2));
}

.viewer-black.viewer-down {
  border-color: rgba(255, 64, 87, 0.72);
  filter: drop-shadow(0 0 12px rgba(255, 64, 87, 0.2));
}

#total_count.count-up {
  animation: total-up 650ms cubic-bezier(0.2, 0.9, 0.3, 1);
}

#total_count.count-down {
  animation: total-down 650ms ease-out;
}

.platform-item.platform-up {
  animation: platform-gain 600ms cubic-bezier(0.2, 0.9, 0.3, 1);
}

.platform-item.platform-down {
  animation: platform-loss 520ms ease-out;
}

@keyframes live-pulse {
  0% {
    box-shadow: 0 0 0 0 rgba(255, 64, 87, 0.55);
  }
  65% {
    box-shadow: 0 0 0 8px rgba(255, 64, 87, 0);
  }
  100% {
    box-shadow: 0 0 0 0 rgba(255, 64, 87, 0);
  }
}

@keyframes widget-enter {
  from {
    opacity: 0;
    transform: translateY(14px) scale(0.96);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

@keyframes hud-float {
  0%, 100% {
    transform: translateY(0);
    box-shadow:
      0 18px 44px rgba(0, 0, 0, 0.52),
      inset 0 0 0 1px rgba(255, 255, 255, 0.045);
  }
  50% {
    transform: translateY(-4px);
    box-shadow:
      0 24px 52px rgba(0, 0, 0, 0.62),
      0 0 16px rgba(255, 255, 255, 0.08),
      inset 0 0 0 1px rgba(255, 255, 255, 0.08);
  }
}

@keyframes live-badge {
  0%, 100% {
    border-color: rgba(255, 255, 255, 0.36);
    box-shadow: 0 0 0 rgba(255, 64, 87, 0);
  }
  50% {
    border-color: rgba(255, 82, 105, 0.72);
    box-shadow: 0 0 14px rgba(255, 64, 87, 0.18);
  }
}

@keyframes icon-float {
  0%, 100% { transform: translateY(0) scale(1); }
  50% { transform: translateY(-2px) scale(1.08); }
}

@keyframes platform-enter {
  from {
    opacity: 0;
    transform: translateY(8px) scale(0.9);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

@keyframes light-sweep {
  0%, 68% {
    left: -38%;
    opacity: 0;
  }
  72% {
    opacity: 1;
  }
  86%, 100% {
    left: 125%;
    opacity: 0;
  }
}

@keyframes total-up {
  0% { color: #ffffff; transform: translateY(0) scale(1); }
  42% { color: #53fc18; transform: translateY(-5px) scale(1.28); text-shadow: 0 0 16px rgba(83, 252, 24, 0.7); }
  100% { color: #ffffff; transform: translateY(0) scale(1); }
}

@keyframes total-down {
  0% { color: #ffffff; transform: translateX(0); }
  30% { color: #ff4057; transform: translateX(-3px); text-shadow: 0 0 14px rgba(255, 64, 87, 0.65); }
  55% { transform: translateX(3px); }
  100% { color: #ffffff; transform: translateX(0); }
}

@keyframes platform-gain {
  0% { transform: scale(1); border-color: rgba(255, 255, 255, 0.44); }
  45% { transform: translateY(-4px) scale(1.12); border-color: rgba(83, 252, 24, 0.8); box-shadow: 0 0 16px rgba(83, 252, 24, 0.28); }
  100% { transform: scale(1); border-color: rgba(255, 255, 255, 0.44); }
}

@keyframes platform-loss {
  0% { transform: translateX(0); border-color: rgba(255, 255, 255, 0.44); }
  30% { transform: translateX(-3px); border-color: rgba(255, 64, 87, 0.78); }
  55% { transform: translateX(3px); }
  100% { transform: translateX(0); border-color: rgba(255, 255, 255, 0.44); }
}

@media (max-width: 620px) {
  .viewer-black {
    gap: 12px;
    padding: 13px;
  }

  .total-label,
  .divider {
    display: none;
  }

  .total-viewers {
    display: flex;
    min-width: auto;
  }

  .platform-item {
    min-width: 52px;
    padding: 7px 9px;
  }
}

@media (prefers-reduced-motion: reduce) {
  #viewer_widget,
  .platform-item,
  .live-dot,
  .live-status,
  .platform-icon,
  .platform-img,
  .viewer-black::after,
  .viewer-black,
  #total_count {
    animation: none !important;
  }
}